name: Metrics
on:
  schedule: [{cron: "0 */12 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          
          # General settings
          user: TimyVillarmia
          template: terminal
          base: header, activity, community, repositories
          filename: github-metrics.svg
          config_timezone: Asia/Manila
          
          # Visual Layout (Fixed height to prevent clipping)
          config_height: 700
          config_padding: 0, 10
          config_order: base.header, languages, habits, achievements
          
          # This pulls your LinkedIn/Email/Bio into 'whoami'
          plugin_introduction: yes

          # Languages (Focus on Logic)
          plugin_languages: yes
          plugin_languages_details: percentage
          # Excluded HTML/CSS to highlight C# and JS
          plugin_languages_ignored: html, css, scss, sass, less
          plugin_languages_limit: 8
          
          # Habits & Achievements
          plugin_habits: yes
          plugin_habits_facts: yes
          plugin_habits_charts: no
          plugin_achievements: yes
          plugin_achievements_display: compact
